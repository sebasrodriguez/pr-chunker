{
  "name": "pr-chunker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "release": "npm run prepare && git add . && git commit -m 'wip: Updating action' && git push",
    "prepare": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/exec": "^1.1.0",
    "@actions/io": "^1.1.1",
    "@octokit/request": "^5.6.0",
    "@vercel/ncc": "^0.28.6",
    "danger": "^10.6.4"
  }
}
